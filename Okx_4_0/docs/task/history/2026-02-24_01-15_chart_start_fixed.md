# 验收文档：图表起始点优化与版本升级 (Build-016)

## 变更总结
根据您的反馈，我们调整了图表的起始点逻辑，并完成了版本迭代。

### 1. 调整图表起始点 (Start from Now)
- **改动**: 在 `run_live_grid.py` 的 `warmup` 阶段，移除了为每一根历史 K 线生成虚拟 RSI 和资产数据的逻辑。
- **效果**: RSI 和资产曲线现在将不再回填 200 根历史点，而是严格地从**系统上线后的第一个实时点 (T0)** 开始绘制。这避免了因虚拟历史数据导致的水平对齐干扰，更符合实时监控的直觉。

### 2. 代码清理与架构优化
- **后端持久化**: 依然保留了 `dashboard.py` 中的历史持久化能力，后续实时产生的数据在页面刷新后依然会被保留，但系统启动前的历史区间将保持为空。
- **批量处理**: 前端的 `prepareBatchData` 继续发挥作用，确保随着时间推移积累的实时数据保持正确的顺序。

### 3. 版本升级
- **版本号**: V4.3
- **构建 ID**: 2026-02-24-Build-016
- **更新描述**: 移除了历史回填对齐，改为从此刻开始绘制。

## 验证结论
- [x] K 线图表正常显示 200 根历史数据。
- [x] RSI 和资产曲线图表在启动初期保持为空（或仅有一个点），并随着 Tick 推移从右侧开始向左生长。
- [x] 动态网格线绘制正常，且不随重绘闪烁。

render_diffs(file:///c:/Projects/CTS1/run_live_grid.py)
render_diffs(file:///c:/Projects/CTS1/templates/dashboard.html)
